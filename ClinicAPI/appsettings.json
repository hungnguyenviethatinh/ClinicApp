{
  "ConnectionStrings": {
    "DefaultConnection": "Server=HUNGNGUYENVIET\\SQLEXPRESS2017;Database=ClinicDb;Trusted_Connection=True;MultipleActiveResultSets=true"
  },

  "ClientSecret": "secret",

  "Kestrel": {
    "Endpoints": {
      // Change to your url and port: http://192.168.100.200:8000 or http://mydomain.com:8000.
      "Http": {
        "Url": "http://localhost:8000"
      },
      // Change to your url and port: https://192.168.100.200:8080 or http://mydomain.com:8080.
      // Comment https config if got certificate error or add a ssl certificate if use https.
      "Https": {
        "Url": "https://localhost:8080"
      }
    }
  },

  // Change to your url and port: http://192.168.100.200:8000 or http://mydomain.com:8000.
  "ApplicationUrl": "http://localhost:8000",
  // Change to your https port, the same port as Endpoints.
  "https_port": 8080,

  "Logging": {
    "PathFormat": "Logs/clinicapp-{Date}.log",
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
}
